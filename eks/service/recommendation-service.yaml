apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service
  namespace: myflix
  labels:
    app: recommendation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recommendation-service
  template:
    metadata:
      labels:
        app: recommendation-service
    spec:
      containers:
        - name: recommendation-service
          image: 723300665462.dkr.ecr.ap-south-1.amazonaws.com/myflix/recommendation-service:v1
          ports:
            - containerPort: 8087
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 200Mi
          # No /health endpoint exists in this service — tcpSocket just
          # confirms the process is listening, not full app health.
          # Real fix would be adding a proper /health endpoint later.
          livenessProbe:
            tcpSocket:
              port: 8087
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 8087
            initialDelaySeconds: 5
            periodSeconds: 10
          envFrom:
            - configMapRef:
                name: myflix-common-config
            - secretRef:
                name: myflix-database-secret
            - secretRef:
                name: myflix-jwt-secret
          env:
            - name: PORT
              value: "8087"
---
apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: myflix
spec:
  selector:
    app: recommendation-service
  ports:
    - port: 8087
      targetPort: 8087
