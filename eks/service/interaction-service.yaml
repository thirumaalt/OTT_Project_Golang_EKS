apiVersion: apps/v1
kind: Deployment
metadata:
  name: interaction-service
  namespace: myflix
  labels:
    app: interaction-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: interaction-service
  template:
    metadata:
      labels:
        app: interaction-service
    spec:
      containers:
        - name: interaction-service
          image: 723300665462.dkr.ecr.ap-south-1.amazonaws.com/myflix/interaction-service:latest
          ports:
            - containerPort: 8089
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 200Mi
          # No /health endpoint exists in this service — tcpSocket just
          # confirms the process is listening, not full app health.
          # Real fix would be adding a proper /health endpoint later.
          livenessProbe:
            tcpSocket:
              port: 8089
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 8089
            initialDelaySeconds: 5
            periodSeconds: 10
          envFrom:
            - configMapRef:
                name: myflix-common-config
            - secretRef:
                name: myflix-database-secret
            - secretRef:
                name: myflix-jwt-secret
          env:
            - name: PORT
              value: "8089"
---
apiVersion: v1
kind: Service
metadata:
  name: interaction-service
  namespace: myflix
spec:
  selector:
    app: interaction-service
  ports:
    - port: 8089
      targetPort: 8089
