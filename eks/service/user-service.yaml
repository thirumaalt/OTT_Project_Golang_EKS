apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: myflix
  labels:
    app: user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: 025211337216.dkr.ecr.ap-south-1.amazonaws.com/myflix/user-service:latest
          ports:
            - containerPort: 8085
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 200Mi
          # No /health endpoint exists in this service — tcpSocket just
          # confirms the process is listening, not full app health.
          # Real fix would be adding a proper /health endpoint later.
          livenessProbe:
            tcpSocket:
              port: 8085
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 8085
            initialDelaySeconds: 5
            periodSeconds: 10
          envFrom:
            - configMapRef:
                name: myflix-common-config
            - secretRef:
                name: myflix-database-secret
            - secretRef:
                name: myflix-jwt-secret
          env:
            - name: PORT
              value: "8085"
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: myflix
spec:
  selector:
    app: user-service
  ports:
    - port: 8085
      targetPort: 8085
