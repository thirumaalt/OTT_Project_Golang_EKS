apiVersion: apps/v1
kind: Deployment
metadata:
  name: subscription-service
  namespace: myflix
  labels:
    app: subscription-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: subscription-service
  template:
    metadata:
      labels:
        app: subscription-service
    spec:
      containers:
        - name: subscription-service
          image: 025211337216.dkr.ecr.ap-south-1.amazonaws.com/myflix/subscription-service:latest
          ports:
            - containerPort: 8093
          envFrom:
            - configMapRef:
                name: myflix-common-config
            - secretRef:
                name: myflix-database-secret
            - secretRef:
                name: myflix-jwt-secret
          env:
            - name: PORT
              value: "8093"
---
apiVersion: v1
kind: Service
metadata:
  name: subscription-service
  namespace: myflix
spec:
  selector:
    app: subscription-service
  ports:
    - port: 8093
      targetPort: 8093