apiVersion: apps/v1
kind: Deployment
metadata:
  name: subscription-service
  namespace: myflix
  labels:
    app: subscription-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: subscription-service
  template:
    metadata:
      labels:
        app: subscription-service
    spec:
      containers:
        - name: subscription-service
          image: 723300665462.dkr.ecr.ap-south-1.amazonaws.com/myflix/subscription-service:v1
          ports:
            - containerPort: 8093
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 200Mi
          # No /health endpoint exists in this service — tcpSocket just
          # confirms the process is listening, not full app health.
          # Real fix would be adding a proper /health endpoint later.
          livenessProbe:
            tcpSocket:
              port: 8093
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 8093
            initialDelaySeconds: 5
            periodSeconds: 10
          envFrom:
            - configMapRef:
                name: myflix-common-config
            - secretRef:
                name: myflix-database-secret
            - secretRef:
                name: myflix-jwt-secret
          env:
            - name: PORT
              value: "8093"
---
apiVersion: v1
kind: Service
metadata:
  name: subscription-service
  namespace: myflix
spec:
  selector:
    app: subscription-service
  ports:
    - port: 8093
      targetPort: 8093
